server:
  port: 8081

spring:
  application:
    name: portal
  datasource:
    url: jdbc:postgresql://localhost:5432/pet_portal
    username: petadmin
    password: pet123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: validate
    open-in-view: false